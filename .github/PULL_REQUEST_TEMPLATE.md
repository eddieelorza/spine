## What and why

## Checklist

- [ ] `npm test` passes (77+ tests, zero failures)
- [ ] If `src/core` or `spec/rules` changed: added/updated a fixture
- [ ] If `site/` changed: `npm run build` inside `site/` succeeds and `docs/` was regenerated
- [ ] No direct edits to `docs/` (it's generated — edit `site/` instead)
